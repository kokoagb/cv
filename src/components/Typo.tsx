import styled from "styled-components";

const H1 = styled.h1`
  color: var(--accent);
  font-size: 3rem;
  line-height: 1;
`;

const H2 = styled.h2`
  color: var(--accent);
  margin-bottom: 1rem;
  border-bottom: 1px solid var(--rule);
`;

const H3 = styled.h3`
  color: var(--accent);
`;

export const Typo = {
  H1,
  H2,
  H3,
};
